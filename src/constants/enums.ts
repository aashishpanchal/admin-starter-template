export enum Role {
  ADMIN = "Admin",
  SELLER = "Seller",
  USER = "User",
}

export enum TokenType {
  REFRESH = "refresh",
  ACCESS = "access",
}

export enum AsyStatus {
  IDLE = "idle",
  ERROR = "error",
  LOADING = "loading",
}
